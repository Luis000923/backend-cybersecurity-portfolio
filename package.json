{
  "name": "backend-cybersecurity-portfolio",
  "version": "1.0.0",
  "private": true,
  "description": "Monorepo de portafolio con Astro y Express",
  "scripts": {
    "dev": "pnpm --filter frontend dev & pnpm --filter backend dev",
    "dev:frontend": "pnpm --filter frontend dev",
    "dev:backend": "pnpm --filter backend dev",
    "build": "pnpm --filter frontend build && rm -rf dist && cp -r frontend/dist ./dist && pnpm --filter backend build",
    "start": "pnpm --filter backend start"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "overrides": {
    "esbuild": "npm:esbuild-wasm@0.24.2",
    "vite": "^6.0.0"
  },
  "packageManager": "pnpm@10.33.2",

  "pnpm": {
    "overrides": {
      "esbuild": "npm:esbuild-wasm@0.24.2",
      "vite": "^6.0.0"
    },
    "onlyBuiltDependencies": [
      "sharp"
    ]
  }
}
